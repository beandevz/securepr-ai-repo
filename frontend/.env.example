# Option A (recommended for dev): proxy mode to avoid CORS
VITE_API_BASE_URL=/api
VITE_PROXY_TARGET=http://localhost:8000

# Option B: direct mode (if backend enables CORS)
# VITE_API_BASE_URL=http://localhost:8000
